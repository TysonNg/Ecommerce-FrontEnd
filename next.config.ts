import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['cdn.pixabay.com','pixabay.com','img.freepik.com','themewagon.github.io','websitedemos.net']
  }
};

export default nextConfig;
